<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lijwen.dao.SuccessKilledMapper">
	<update id="insertSuccessKilled">
		insert into
		success_killed(seckill_id,user_phone,state)
		values (
		#{seckillId},#{userPhone},0)
	</update>

	<select id="queryByIdWithSeckill" resultType="SuccessKilled">
		<!-- 根据ID查询SuccessKilled 并携带秒杀商品对象实体 -->
		<!-- 如果告诉Mybatis把结果映射到SuccessKilled同时映射到sekill属性可以自由控制sql -->
		select
		sk.seckill_id,
		sk.user_phone,
		sk.create_time,
		s.state,
		s.seckill_id
		"seckill.seckill_id",
		s.name "seckill.name",
		s.number "seckill.number",
		s.start_time "seckill.start_time",
		s.end_time "seckill.end_time",
		s.create_time "seckill.create_time"
		from success_killed sk
		inner join
		seckill s on
		sk.seckill_id = s.seckill_id
		where sk.seckill_id =
		#{seckillId} and
		sk.user_phone = #{userPhone}
	</select>

</mapper>